@model Blog.Models.Comment
@{
    ViewData["Title"] = "DeleteComment";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this comment?</h3>
<div>
    <h4>Comment</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Model.CommentAuthor)
        </dt>
        <dd>
            @Model.CommentAuthor)
        </dd>
        <dt>
            @Model.CommentBody)
        </dt>
        <dd>
            @Model.CommentBody)
        </dd>
        <dt>
            @Model.CommentDate)
        </dt>
        <dd>
            @Model.CommentDate)
        </dd>

    </dl>
    
    <form asp-action="DeleteCommentConfirmed" asp-route-id="@Model.ID" asp-route-PostID="@Model.PostID">
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-warning" /> |
            <a asp-action="Index">Back to List</a>
        </div>
    </form>
</div>
